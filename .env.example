### Solana / Anchor
RPC_ENDPOINT=https://api.devnet.solana.com
KEYPAIR_PATH=~/.config/solana/id.json

### Switchboard
SWITCHBOARD_VRF_QUEUE=
SWITCHBOARD_SCORE_FEED=

### LLM Keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

### PostgreSQL Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=elizaos

### Proton Bridge SMTP (inside container)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=bridge_user
SMTP_PASS=bridge_pass

### Phase-2 additions
CLOCKWORK_THREAD_ID=              # set by create_thread.ts
CERAMIC_DID_SEED=                 # 32 byte hex
AKASH_DEPLOY_KEY=                 # wallet for deploy

### WebSocket Server
WS_PORT=8080
WS_HOST=localhost

### Email Configuration
FROM_EMAIL=no-reply@footballsquares.app
WINNER_EMAIL_TEMPLATE=winner-notification

### Oracle Configuration
SCORE_POLL_INTERVAL=30000         # 30 seconds
GAME_CHECK_INTERVAL=300000        # 5 minutes

### UI / Hero media
# Provide a local or hosted MP4 (preferred) or GIF for the Home hero background.
# For a local asset, place the file under public/assets and set the path below.
# Example: /assets/hero.mp4
NEXT_PUBLIC_HERO_MEDIA_URL=
# Optional Tenor fallback post id (unused when NEXT_PUBLIC_HERO_MEDIA_URL is set)
NEXT_PUBLIC_TENOR_POSTID=
