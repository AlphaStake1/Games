{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BoardAgent",
  "description": "An agent for managing the football squares board on-chain.",
  "type": "object",
  "properties": {
    "createBoard": {
      "type": "object",
      "description": "Creates a new football squares board.",
      "properties": {},
      "required": []
    }
  },
  "required": ["createBoard"]
}